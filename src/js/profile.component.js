'use strict';

module.exports = {
    templateUrl: '../template/profile.template.html',
    controller: profileComponent,
    bindings: {
        profile: '<'
    }
};

function profileComponent() {
    var self = this;
}